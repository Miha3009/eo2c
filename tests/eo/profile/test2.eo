+package profile

[] > test2
  QQ.io.stdout (QQ.txt.sprintf "%d" (paths-count 8 8)) > @

  [n m] > paths-count
    if. > @
      or.
        eq.
          n
          1
        eq.
          m
          1
      1
      plus.
        paths-count (n.minus 1) m
        paths-count n (m.minus 1)
